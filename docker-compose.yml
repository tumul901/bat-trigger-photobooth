services:
  photobooth:
    build: .
    container_name: photobooth
    ports:
      - "80:80"
      - "443:443"
      - "9999:9999"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    restart: always
